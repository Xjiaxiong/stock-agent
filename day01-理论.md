##01 什么是 Agent ?

答：Agent 是一个拥有状态，可以调用工具的LLM，拥有完整的感知环境，计划，执行和观察并反馈的完整做事流程。

##02 Agent和 Chatbot 有什么区别?

答： Agent 的职责是解决问题，而 Chatbot 的职责是生成回答； 前者通过调用工具与外部世界联系，而后者仅仅到生成回答的内容就结束了。

##03 Agent 和 Workflow 有什么区别?

答：Agent 是灵活的，自主决策的，有点不可控； 而Workflow 是有固定流程的，有可预测的，有可复用的。 他们之间可以混合使用。

##04 为什么 Agent 需要 Tool?

答： Agent 当中的LLM 决策的时候需要工具调用结果，如果没有工具调用结果的反馈，很多大型任务是无法自拔很好的完成的。 LLM 是静态的，无法获取到实时的信息，只有调用工具动作，模型才能更具反馈的结果最终去完成任务。

##05 什么时候应该用普通 LLM Application 而不是 Agent ?

答： 如果你的意图仅仅是是基于固定资料的LLM 应用，那么你可以使用普通 LLM Application；而不是 使用 Agent。因为你整体不涉及工具调用，观察反馈继续、计划、执行和观察反馈的循环。